# Единственная зависимость приложения.
# matplotlib и numpy не нужны: график рисуется средствами Kivy, а вычисления
# идут на чистом Python — это заметно облегчает сборку APK.
kivy>=2.3,<3

# Для запуска тестов:
#   pip install pytest
#   xvfb-run -a python -m pytest        (тесты интерфейса требуют дисплея)
